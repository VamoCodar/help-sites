function trocaDireito() { contador <= 2 && anterior.getAttribute("data-target", "block") ? (anterior.setAttribute("data-target", "none"), proxima.setAttribute("data-target", "block"), contador += 1, contadorAnterior += 1) : (anterior.setAttribute("data-target", "none"), primeira.setAttribute("data-target", "block"), contador = 1, contadorAnterior = 0); proxima = document.querySelector(`.card${contador}`); anterior = document.querySelector(`.card${contadorAnterior}`); console.log(contador, contadorAnterior); conta() } function trocaEsquerda() { contadorAnterior > 0 && (contador -= 1, contadorAnterior -= 1); proxima = document.querySelector(`.card${contador}`); anterior = document.querySelector(`.card${contadorAnterior}`); anterior.setAttribute("data-target", "block"); proxima.setAttribute("data-target", "none"); console.log(contador, contadorAnterior); anterior.setAttribute("data-target", "animacao"); conta() } function conta() { counter.innerHTML = `${contador}/3` } function initScroolSuave() { function n(n) { n.preventDefault(); const t = n.currentTarget.getAttribute("href"), i = document.querySelector(t); i.scrollIntoView({ behavior: "smooth", block: "start" }) } const t = document.querySelector(".arrow "), i = document.querySelector(".arrow-up "); t.addEventListener("click", n); i.addEventListener("click", n) } function initScroolSuave2() { function t(n) { n.preventDefault(); const t = n.currentTarget.getAttribute("href"), i = document.querySelector(t); i.scrollIntoView({ behavior: "smooth", block: "start" }) } const n = document.querySelectorAll(".menu-open ul li"); n.forEach(n => n.addEventListener("click", t)) } function active() { body.classList.toggle("ativo") } function fecha(n) { n.target != menuOpen && n.target != menu && (menuOpen.setAttribute("style", "animation: opaciti-volta2 300ms both ease-out;"), setTimeout(() => { body.classList.remove("ativo"), menuOpen.removeAttribute("style", "animation") }, 300)) } let contadorAnterior = 0, contador = 1, primeira = document.querySelector(".card0 "), proxima = document.querySelector(`.card${contador}`), anterior = document.querySelector(`.card${contadorAnterior}`); const botaoDireito = document.querySelectorAll(".right"), botaoEsquerdo = document.querySelectorAll(".left"); let counter = document.querySelector(".counter"); initScroolSuave(); initScroolSuave2(); let menu = document.querySelector(".menu "), body = document.querySelector("body"), menuOpen = document.querySelector(".menu-open "), intro = document.querySelector(".intro "); menu.addEventListener("click", active); document.addEventListener("click", fecha); botaoDireito.forEach(n => n.addEventListener("click", trocaDireito)); botaoEsquerdo.forEach(n => n.addEventListener("click", trocaEsquerda))